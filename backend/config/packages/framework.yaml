# see https://symfony.com/doc/current/reference/configuration/framework.html
framework:
    secret: '%env(APP_SECRET)%'

    # API sans état (jeton Bearer, firewall stateless) : aucune session PHP, donc aucun cookie de session.
    session: false

    #esi: true
    #fragments: true

when@test:
    framework:
        test: true
